name: fuzzintest
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State management
  get: ^4.6.6
  video_player: ^2.8.1
  flutter_cache_manager: ^3.3.1
  shimmer: ^3.0.0
  path_provider: ^2.1.1
  dio: ^5.3.2
  cached_network_image: ^3.3.0
  visibility_detector: 0.4.0+2


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
  # Add local assets if needed
  # - assets/videos/
